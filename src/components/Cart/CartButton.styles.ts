import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginLeft: 15,
  },
  badge: {
    position: 'absolute',
    backgroundColor: '#2B6CB0',
    top: -10,
    right: -10,
    padding: 3,
    borderRadius: 10,
  },
  badgeText: {
    fontSize: 10,
    color: '#fff',
  },
});
