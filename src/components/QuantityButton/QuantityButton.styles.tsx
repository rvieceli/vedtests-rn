import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  quantity: {
    paddingHorizontal: 5,
    color: '#718096',
    fontWeight: '500',
  },
});
